spring:
  profiles:
    active: local
  application:
    name: group-service
  cloud:
    nacos:
      server-addr: 192.168.85.139:8840
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-okhttp.yaml
          - dataId: shared-jdbc.yaml